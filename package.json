{
  "name": "@welt-io/strands",
  "version": "0.0.0",
  "description": "The Strands Agents (TypeScript) adapter for Welt's wire contract.",
  "license": "MIT",
  "author": "Takashi Iwamoto",
  "homepage": "https://github.com/iwamot/welt-io-strands-ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iwamot/welt-io-strands-ts.git"
  },
  "bugs": {
    "url": "https://github.com/iwamot/welt-io-strands-ts/issues"
  },
  "keywords": [
    "welt",
    "strands",
    "slack",
    "agent",
    "adapter"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "check": "biome check",
    "check:write": "biome check --write",
    "prune": "pnpm-override-prune --fix",
    "typecheck": "tsc --noEmit",
    "test": "mkdir -p coverage && node --test --experimental-test-coverage --test-coverage-include='src/*.ts' --test-coverage-lines=100 --test-coverage-functions=100 --test-coverage-branches=100 --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info 'tests/*.test.ts'"
  },
  "peerDependencies": {
    "@strands-agents/sdk": ">=1.2.0"
  },
  "devDependencies": {
    "@strands-agents/sdk": "1.15.0",
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
