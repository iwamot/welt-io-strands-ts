name: Compatibility

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-node.yml@45ef58fbb304855f7201a3d2c5d8f892934e533e # v8.9.0
    with:
      node-versions: '["22","24"]'
